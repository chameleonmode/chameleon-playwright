interface Config {
  cdpPort: number;
  // Add other configuration properties here
}

export const config: Config = {
  cdpPort: 9222,  // Default value, adjust as needed
  // Add other configuration properties here
};
